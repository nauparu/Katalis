import { Settings, type SettingsOptions } from '../states/settings.svelte.js';
/** Get the current settings context, or default if not set */
export declare function getSettings(): Settings;
export declare function setSettings(settings: SettingsOptions): Settings;
